#include<iostream>
using namespace std;

int main(){
    /*
    int arr[5] = {10, 20, 30, 40, 50}; // Array Intialization
    int arr2[4];
    
    fill(arr2,arr2+4,23); // to fill elements in array

    cout << arr2[0] << " " << arr2[1];
    */

    /*
    // storing element in array using for loop
    int arr[5];

    for(int i = 0; i < 5; i++){
        cin >> arr[i];
    }

    //printing elements of array using for loop
    for(int i = 0; i < 5; i++){
        cout << arr[i] << endl;
    }
    */
    
    
    

    
    return 0;
}