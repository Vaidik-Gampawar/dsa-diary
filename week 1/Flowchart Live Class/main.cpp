#include<iostream>
using namespace std;

int main(){
    //Sum of n numbers
    /*
    int n;
    int sum = 0;
    cin >> n;

    for(int i = 1; i <= n; i++){
        int value;
        cout << "Enter value to be added" << endl;
        cout << i << " Value : ";
        cin >> value;

        sum = sum + value;
        cout << "Sum total : " << sum << endl;
    }
    */

    //Print even number
    /*
    int n;
    cin >> n;

    for(int i = 1; i <= n; i++){
        if(i%2 != 0){
            cout << i << " ";
        }
    }
    */

    // neg, pos, zero
    /*
    int num;
    cin >> num;

    if(num == 0){
        cout << num << " is zero.";
    } else if (num > 0)
    {
        cout << num << " is positive.";
    } else {
        cout << num << " is negative";
    }
    */
    
    
    
    
}